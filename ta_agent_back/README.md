# 生成反馈报告
```shell
cd e:\code\ta-agent\ta_agent_back && uv run ./tools.py 
```

# 启动后端
```shell
cd e:\code\ta-agent\ta_agent_back && uv run -m uvicorn main1:app --reload --host 0.0.0.0 --port 8000
```

# 启动前端
```shell
cd e:\code\ta-agent && npm run dev
```
